# React + Vite Frontend

**API Configuration:**

Review the `frontend/src/config/api.js` file and ensure it references environment variables

### Format Frontend Code

```powershell
npx prettier --write .
```
